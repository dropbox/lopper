language: cpp
sudo: required
dist: trusty
compiler:
  - clang
  - gcc
os:
  - linux
before_install:
  - sudo apt-get update
  - sudo apt-get install libgtest-dev
  - "cd /usr/src/gtest && sudo cmake . && sudo cmake --build . && sudo mv libg* /usr/local/lib/ ; cd -"
  - echo $LANG
  - echo $CXX
script: "cd $TRAVIS_BUILD_DIR; mkdir cmake_build; cd cmake_build; cmake ..; make run_test"
branches:
  only:
  - master
notifications:
  email: false
